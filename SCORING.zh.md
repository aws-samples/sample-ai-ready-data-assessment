# 打分与评测机制（完整版）

**🌐 Language / 语言:** [English](./SCORING.md) · **简体中文**

适用范围：45 题版本（维度1 共 6 题、维度2 共 12 题、维度3 共 12 题、维度4 共 5 题、维度5 共 5 题、维度6 共 5 题）。本文件是**打分口径的权威定义**，`scoring/score.py` 严格实现本文件。

---

## 一、单题打分规则

每题只能选择 A / B / C / D / N/A 五个选项中的一个，分值规则如下：

| 选项 | 分值 | 含义 |
|---|---|---|
| A | 0 分 | 未起步——完全没有相关机制，或仅停留在意识层面 |
| B | 1 分 | 初步实践——有局部、零散的尝试，尚未成体系 |
| C | 2 分 | 基本达标——机制已建立，覆盖主要场景，但自动化、验证、闭环仍有缺口 |
| D | 4 分 | 成熟领先——体系化、自动化、可持续验证，是相对于 C 档的质的跃升 |
| N/A | 不计分 | 该题所问的能力/场景在企业当前阶段不存在，不计入该题分值，也不计入所在维度的满分分母 |

**为什么 C 到 D 之间是 2 分的跳跃，而不是等距的 1 分：**

A→B→C 这三档描述的是从“没有”到“有点意识”到“基本成体系”的渐进过程，属于量变；C→D 描述的是从“体系搭起来了”到“体系真正跑通、能自动化运行、能持续验证效果”，这中间跨越的门槛远高于前三档之间任何一步——这是质变，不是简单的再往上多做一点。用等距刻度（0/1/2/3）会把这种质变压平，导致“多数题目卡在 C 档、只有少数题目冲到 D 档”的企业，总分看起来和真正体系化运作的企业很接近，掩盖了两者的本质差距。用 0/1/2/4 这套刻度，能让分数更真实地反映“这家企业是不是真正跑通了闭环，而不是看起来搭了个架子”。

---

## 二、维度得分计算

### 2.1 基础公式

```
维度得分 = Σ sᵢ     （sᵢ 为该维度第 i 道非 N/A 题目的得分：A=0 / B=1 / C=2 / D=4）
```

### 2.2 维度满分与得分率

由于存在 N/A 豁免机制，同一维度在不同企业的“有效满分”会不同，因此**不能直接用固定满分做除法**，必须用“实际作答题数 × 4”作为动态分母：

```
维度得分率 = 维度得分 / (实际作答题数 × 4) × 100%
```

**理论满分参照表**（假设该维度全部题目都有效作答、无 N/A 时的满分，仅供设计参考，不是实际计分依据）：

| 维度 | 题数 | 每题满分 | 理论满分 |
|---|---|---|---|
| 维度1 成本与业务价值 | 6 | 4 | 24 |
| 维度2 数据基础与质量 | 12 | 4 | 48 |
| 维度3 平台架构与运维 | 12 | 4 | 48 |
| 维度4 组织与人才 | 5 | 4 | 20 |
| 维度5 治理与信任 | 5 | 4 | 20 |
| 维度6 安全与合规 | 5 | 4 | 20 |
| **总计** | **45** | — | **180** |

### 2.3 N/A 处理的两种情形

- **维度内部分题目 N/A**：正常按“实际作答题数 × 4”重新计算分母，不影响其他题目计分。
- **维度内全部题目均为 N/A**（例如企业完全没有 Agent 场景，导致维度6 多题全部不适用）：该维度不参与得分率计算和雷达图对比，报告中应明确标注“该维度暂不适用于当前评估对象，建议企业发展到相应阶段后重新评估”，而不是显示为 0%——显示 0% 会被误读为“安全合规做得很差”，但真实情况是“这个维度考察的能力根本还不存在”，两者性质完全不同。

---

## 三、总分计算

### 3.1 总分公式（等权重版，默认）

```
总分       = Σ 维度得分_d                （d = 1..6）
总体得分率 = 总分 / Σ (n_d × 4) × 100%    （n_d = 第 d 维度实际作答题数）
```

默认六个维度按题目数量自然加权（题目多的维度自然占总分比重更大），不需要额外设置维度权重系数。

### 3.2 可选：行业/阶段权重调整（进阶功能）

如果企业所处行业对某些维度有更高要求（例如金融、医疗对“安全与合规”“治理与信任”的要求应高于互联网），可引入维度权重系数 `w_d`：

```
加权总体得分率 = Σ (w_d × 维度得分率_d) / Σ w_d
```

默认所有 `w_d = 1`（等权重），即回归 3.1 的基础公式。是否启用加权，建议作为报告生成时的可选项（脚本 `--weights` 参数），而不是默认行为，避免评估标准因权重设置不同而失去可比性。

---

## 四、成熟度层级划分

层级划分统一按**得分率**（而非原始分数）进行，无论总体层级还是单个维度层级，都套用同一套区间标准（区间为左闭右开 `[lo, hi)`，最高档 L4 含 100%；边界值如恰好 25.0% 归入更高一档 L2）：

| 层级 | 得分率区间 | 含义 |
|---|---|---|
| L1 未起步 | `[0%, 25%)` | AI 相关数据能力基本空白，多数已作答题目落在 A/B，缺乏基础机制 |
| L2 初步实践 | `[25%, 50%)` | 有零散实践和局部工具，但未成体系，依赖个人经验多于制度 |
| L3 基本达标 | `[50%, 75%)` | 核心机制已建立并覆盖主要场景，但自动化、验证、闭环仍有缺口——通常对应“多数题目选 C、少数选 D” |
| L4 成熟领先 | `[75%, 100%]` | 体系化、自动化、可追溯，具备持续改进闭环——只有 D 档占比较高时才能进入此区间 |

由于 C→D 的分值差距被拉大到 2 分（是 A→B、B→C 每一步的 2 倍），**L3 与 L4 之间的实际门槛会更陡峭**：企业要从“基本达标”迈入“成熟领先”，需要多数题目真正冲到 D 档，而不能靠零散几个 D 档“拉高平均分”混过去。

层级标签建议同时生成两层：

1. **总体层级**：基于总体得分率，给出企业 AI-Ready 数据能力的整体定位。
2. **各维度独立层级**：基于每个维度自己的得分率单独判断，帮助管理层一眼看出“哪个维度是短板、哪个维度已经领先”，而不是被总分掩盖结构性失衡。

---

## 五、计分示例（含新旧刻度对比）

假设某企业在维度2（数据基础与质量，12 题）中作答：2 题选 A，2 题选 B，6 题选 C，2 题选 D，0 题 N/A。

```
维度2得分   = 2×0 + 2×1 + 6×2 + 2×4 = 0 + 2 + 12 + 8 = 22 分
维度2得分率 = 22 / (12×4) = 22/48 ≈ 45.8%
```

对照层级表，45.8% 落在 **L2 初步实践** 区间的高段，接近但未到 L3。

**对比旧刻度**（A=0 / B=1 / C=2 / D=3）下的同一份作答：

```
旧刻度得分  = 2×0 + 2×1 + 6×2 + 2×3 = 20 分，满分 36 分，得分率 ≈ 55.6%
```

旧刻度下 55.6% 会被划入 **L3 基本达标**，但新刻度下只有 45.8%，仍停留在 L2。这个差异恰好反映了新刻度的用意：这份作答里 D 档只占 2/12（约 17%），大部分题目还停留在 C 档“基本达标但没有闭环”，新刻度更准确地把它判定为“初步实践的高段”，而不是直接跳级到“基本达标”。

---

## 六、报告呈现建议

- **雷达图**：六个轴分别代表六个维度的得分率（0%–100%），而不是原始分数，确保跨维度可比。
- **总体层级标签**：在雷达图旁标注总体得分率对应的 L1–L4 标签。
- **维度层级标签**：每个轴上同时标注该维度单独的 L1–L4 标签，用于快速定位短板。
- **N/A 维度特别标注**：若某维度因全部 N/A 被排除在雷达图对比之外，需在报告显著位置注明原因，避免被误读为“零分”。
- **D 档占比单独展示**（可选进阶）：除总分和得分率外，额外展示“D 档题目占实际作答题目的比例”，这个指标比单纯的得分率更能直观说明企业有多少能力真正达到了“体系化 + 自动化 + 可验证闭环”的水平，而不是被 C 档的数量“平均”出一个看起来还不错的分数。
- **报告必须带上问卷版本号**：一份结果只与**同一问卷版本**产出的结果可比，因此版本号必须与分数一同流转。`score.py` 会自动标注 —— 人类可读报告中为 `问卷版本: v<x.y.z>`，`--json` 输出中为 `questionnaire_version` 字段，取自 [`VERSION`](./VERSION)。若同比或跨事业部对比跨越了两个问卷版本，必须在报告中明确说明；版本化规则见 [`CONTRIBUTING.zh.md`](./CONTRIBUTING.zh.md#问卷版本化保证跨版本可比)。
